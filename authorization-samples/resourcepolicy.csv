# Resource Policy Configuration

# Any Non Email account is treated as a Resource
# This policy has a two part definition, one set of policies define, which resources can login to the token server
# The second set of policies define, which users can access to the resource server by requesting asymmetric shared key

# The below policy defines which resource has permission to login to the token server for a specific client id
p, myresource.server, mytoken.server, CLIENTID, login

# The below policy defines if a specific resource has permission request for asymmetric shared key of users and can gather information about users
p, myresource.server, /*@/*./*, CLIENTID, ask
p, myresource.server, /*@/*./*, CLIENTID, poke


